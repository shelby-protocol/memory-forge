{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f package.json ]; then npx prettier --write \"${FILE_PATH}\"; else cd ${PWD}/agentvault && npx prettier --write \"${FILE_PATH}\"; fi 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}
